import UpdatePasswordForm from "@/components/UpdatePasswordForm";

export const metadata = { title: "Choose a new password — Expense Flow" };

export default function UpdatePasswordPage() {
  return <UpdatePasswordForm />;
}
